diff --git a/ggml/src/ggml-metal/ggml-metal-device.cpp b/ggml/src/ggml-metal/ggml-metal-device.cpp
index 02bee5e3e..f27e6f95f 100644
--- a/ggml/src/ggml-metal/ggml-metal-device.cpp
+++ b/ggml/src/ggml-metal/ggml-metal-device.cpp
@@ -1208,17 +1208,17 @@ ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mv_pq2_swigl
 // a pair of columns shares one read of each block, where one pass per column would read and
 // unpack the weights twice. Four columns go as two pairs where that measured faster.
 // TOSH_MV_NC2_PROBE=R opens the untuned widths with R rows, for measuring
 static int nc2_probe() {
     static const int r = TOSH_ENV("TOSH_MV_NC2_PROBE") ? atoi(TOSH_ENV("TOSH_MV_NC2_PROBE")) : 0;
     return r;
 }
 
-int ggml_metal_mv_nc2_rows(ggml_type type, int simd_width, int ne11) {
+int ggml_metal_mv_nc2_rows(ggml_type type, int simd_width, int ne11, int ne01) {
     static const bool disabled = TOSH_ENV("TOSH_MV_NC2_DISABLE") != nullptr;
     if (disabled || ne11 < 2 || ne11 > 4) {
         return 0;
     }
     const bool w64 = simd_width == 64;
     // three columns in one pass, where that beat the batched kernel
     if (ne11 == 3) {
         static const bool nc3_off = TOSH_ENV("TOSH_MV_NC3_DISABLE") != nullptr;
@@ -1227,25 +1227,29 @@ int ggml_metal_mv_nc2_rows(ggml_type type, int simd_width, int ne11) {
         }
         switch (type) {
             case GGML_TYPE_Q5_K:    return 4;
             default: return 0;
         }
     }
     switch (type) {
         case GGML_TYPE_Q4_K: return w64 ? (ne11 == 2 ? 8 : 0) : 4;
-        // four columns stay on the batched kernel for these two, which reads them faster
-        case GGML_TYPE_Q4_0:
-        case GGML_TYPE_Q4_1: return w64 ? nc2_probe() : (ne11 == 2 ? 4 : 0);
-        case GGML_TYPE_Q5_0:
-        case GGML_TYPE_Q5_1: return w64 ? nc2_probe() : 4;
-        case GGML_TYPE_Q8_0: return w64 ? nc2_probe() : 0;
+        // on 32 lanes four columns of q4_0/q4_1 stay on the batched kernel, which reads them
+        // faster; on 64 lanes eight rows per simdgroup beat it at two and four columns. q4_0 only
+        // keeps eight for a pair where the grid comes out in whole waves or is large enough
+        case GGML_TYPE_Q4_0: return w64 ? (ne11 != 2 || (ne01 % 2048 == 0 && ne01 >= 4096) || ne01 >= 16384 ? 8 : 4)
+                                        : (ne11 == 2 ? 4 : 0);
+        case GGML_TYPE_Q4_1: return w64 ? 8 : (ne11 == 2 ? 4 : 0);
+        case GGML_TYPE_Q5_0: return w64 ? 8 : 4;
+        case GGML_TYPE_Q5_1: return 4;
+        case GGML_TYPE_Q8_0: return w64 ? 8 : 0;
         case GGML_TYPE_IQ3_S: return w64 ? nc2_probe() : 8;
         case GGML_TYPE_IQ3_XXS: return w64 ? nc2_probe() : 8;
-        case GGML_TYPE_Q5_K: return w64 ? nc2_probe() : 4;
+        // 64 lanes only gain on matrices of few rows, with two rows per simdgroup
+        case GGML_TYPE_Q5_K: return w64 ? (nc2_probe() ? nc2_probe() : (ne11 == 2 && ne01 <= 5120 ? 2 : 0)) : 4;
         case GGML_TYPE_Q6_K: return w64 ? nc2_probe() : (ne11 == 2 ? 2 : 0);
         default: return 0;
     }
 }
 
 ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mv(ggml_metal_library_t lib, const ggml_tensor * op) {
     GGML_TENSOR_LOCALS( int32_t, ne0, op->src[0], ne);
     GGML_TENSOR_LOCALS( int32_t, ne1, op->src[1], ne);
@@ -1747,25 +1751,25 @@ ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mv(ggml_meta
             }
 
             if (sns != nullptr) {
                 nsg = atoi(sns);
             }
         }
     }
 
-    if (const int rows = ggml_metal_mv_nc2_rows(tsrc0, simd_width, ne11); rows > 0) {
+    if (const int rows = ggml_metal_mv_nc2_rows(tsrc0, simd_width, ne11, ne01); rows > 0) {
         nr0 = rows;
         nr1 = ne11 == 3 ? 3 : 2;
         nsg = simd_width == 64 ? 1 : 2;
         static thread_local char nc_sfx[16];
         snprintf(nc_sfx, sizeof(nc_sfx), "_c%d_r%d", nr1, rows);
         suffix = nc_sfx;
-        // only q4_K has a 64-lane twin; the others run their one kernel at either width
-        w64 = simd_width == 64 && tsrc0 == GGML_TYPE_Q4_K;
+        // q4_K and q5_K have a 64-lane twin; the others run their one kernel at either width
+        w64 = simd_width == 64 && (tsrc0 == GGML_TYPE_Q4_K || tsrc0 == GGML_TYPE_Q5_K);
         if (const char * sg = TOSH_ENV("TOSH_MV_NC2_SG")) {
             nsg = atoi(sg);
         }
     }
 
     char sfx[64];
     if (w64) {
         snprintf(sfx, sizeof(sfx), "%s_w64", suffix);
diff --git a/ggml/src/ggml-metal/ggml-metal-device.h b/ggml/src/ggml-metal/ggml-metal-device.h
index fa6fb9029..3e7651433 100644
--- a/ggml/src/ggml-metal/ggml-metal-device.h
+++ b/ggml/src/ggml-metal/ggml-metal-device.h
@@ -140,17 +140,17 @@ struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_gated_del
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_turbo_wht         (ggml_metal_library_t lib);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_solve_tri         (ggml_metal_library_t lib, const struct ggml_tensor * op);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mv_ext        (ggml_metal_library_t lib, const struct ggml_tensor * op, int nsg, int nxpsg, int r1ptg, int nr0);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm            (ggml_metal_library_t lib, const struct ggml_tensor * op);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mv            (ggml_metal_library_t lib, const struct ggml_tensor * op);
 
 // rows per simdgroup of the few-column mat-vec for this type, lane width and column count,
 // 0 if the batch does not take it
-int ggml_metal_mv_nc2_rows(enum ggml_type type, int simd_width, int ne11);
+int ggml_metal_mv_nc2_rows(enum ggml_type type, int simd_width, int ne11, int ne01);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm_id_map0    (ggml_metal_library_t lib, int ne02, int ne20);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm_id_amax(ggml_metal_library_t lib);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm_id_amax_part(ggml_metal_library_t lib);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mm_id         (ggml_metal_library_t lib, const struct ggml_tensor * op);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_mul_mv_id         (ggml_metal_library_t lib, const struct ggml_tensor * op);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_argmax            (ggml_metal_library_t lib, const struct ggml_tensor * op);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_tosh_topk_moe     (ggml_metal_library_t lib);
 struct ggml_metal_pipeline_with_params ggml_metal_library_get_pipeline_argsort           (ggml_metal_library_t lib, const struct ggml_tensor * op);
diff --git a/ggml/src/ggml-metal/ggml-metal-ops.cpp b/ggml/src/ggml-metal/ggml-metal-ops.cpp
index 773c14e5c..0e30dd320 100644
--- a/ggml/src/ggml-metal/ggml-metal-ops.cpp
+++ b/ggml/src/ggml-metal/ggml-metal-ops.cpp
@@ -3408,17 +3408,17 @@ int ggml_metal_op_mul_mat(ggml_metal_op_t ctx, int idx) {
     // first try to use small-batch mat-mv kernels
     // these should be efficient for BS [2, ~8]
     // ext kernel derives its row split from the simdgroup width, so it runs on both
     // wave32 and wave64. wave64 rides on wave64_decode (matches the mat-vec gating).
     static const bool ext_disabled = TOSH_ENV("TOSH_EXT_DISABLE") != nullptr;
     if (!ext_disabled &&
         (props_dev->simd_width == 32 || (props_dev->simd_width == 64 && props_dev->wave64_decode)) &&
         !(props_dev->simd_width == 64 && ext_loses_w64(op->src[0]->type, ne11)) &&
-        ggml_metal_mv_nc2_rows(op->src[0]->type, props_dev->simd_width, ne11) == 0 &&
+        ggml_metal_mv_nc2_rows(op->src[0]->type, props_dev->simd_width, ne11, ne01) == 0 &&
         op->src[1]->type == GGML_TYPE_F32 && (ne00%128 == 0) &&
         (
          (
           (
            op->src[0]->type == GGML_TYPE_F32  || // TODO: helper function
            op->src[0]->type == GGML_TYPE_F16  ||
            op->src[0]->type == GGML_TYPE_BF16 ||
            op->src[0]->type == GGML_TYPE_Q1_0 ||
diff --git a/ggml/src/ggml-metal/kernels/mul_mv_w64.metal b/ggml/src/ggml-metal/kernels/mul_mv_w64.metal
index b391826b3..fbc51861c 100644
--- a/ggml/src/ggml-metal/kernels/mul_mv_w64.metal
+++ b/ggml/src/ggml-metal/kernels/mul_mv_w64.metal
@@ -1032,16 +1032,173 @@ kernel void kernel_mul_mv_q4_K_f32_c##NC##_r##NR0##_w64( \
         uint3  tgpig[[threadgroup_position_in_grid]], \
         ushort tiisg[[thread_index_in_simdgroup]], \
         ushort sgitg[[simdgroup_index_in_threadgroup]]) { \
     kernel_mul_mv_q4_K_f32_nc_w64_impl<NR0, NC>(args, src0, src1, dst, tgpig, tiisg, sgitg); \
 }
 
 Q4_K_MV_NC_W64(8, 2)
 
+// q5_K with a few columns: the one-column 64-lane decomposition, each row's weights decoded
+// once per block and applied to every column
+template<int NR0, int NC>
+void kernel_mul_mv_q5_K_f32_nc_w64_impl(
+        constant ggml_metal_kargs_mul_mv & args,
+        device const char * src0,
+        device const char * src1,
+        device       char * dst,
+        uint3  tgpig,
+        ushort tiisg,
+        ushort sgitg) {
+    const short NSG = FC_mul_mv_nsg;
+
+    const int nb = args.ne00/QK_K;
+
+    const int r0 = tgpig.x;
+    const int r1 = tgpig.y*NC;
+    const int im = tgpig.z;
+
+    const int first_row = (r0 * NSG + sgitg) * NR0;
+
+    const uint i12 = im%FC_mul_mv_ne12;
+    const uint i13 = im/FC_mul_mv_ne12;
+
+    const uint64_t offset0 = first_row*args.nb01 + (i12/FC_mul_mv_r2)*args.nb02 + (i13/FC_mul_mv_r3)*args.nb03;
+
+    device const block_q5_K * x = (device const block_q5_K *) (src0 + offset0);
+
+    constexpr uint16_t kmask1 = 0x3f3f;
+    constexpr uint16_t kmask2 = 0x0f0f;
+    constexpr uint16_t kmask3 = 0xc0c0;
+
+    const short NG  = FC_mul_mv_nw/16;
+    const short tid = tiisg%16;
+    const short ix  = tiisg/16;
+    const short iq  = tid/8;
+    const short ir  = tid%8;
+
+    const short l0 = 4*ir;
+    const short q_offset = 32*iq + l0;
+    const short y_offset = 64*iq + l0;
+
+    const ushort hsh = 2*iq;
+    const ushort shs = 16*iq;
+
+    device const float * yc[NC];
+    FOR_UNROLL (short c = 0; c < NC; c++) {
+        const int col = min(r1 + c, (int) args.ne11 - 1);
+        yc[c] = (device const float *) (src1 + col*args.nb11 + i12*args.nb12 + i13*args.nb13) + y_offset;
+    }
+
+    float sumf[NR0][NC];
+    FOR_UNROLL (short row = 0; row < NR0; row++) {
+        FOR_UNROLL (short c = 0; c < NC; c++) {
+            sumf[row][c] = 0.f;
+        }
+    }
+
+    for (int i = ix; i < nb; i += NG) {
+        float yl[NC][8], yh[NC][8];
+        float4 sumy[NC];
+        FOR_UNROLL (short c = 0; c < NC; c++) {
+            device const float * y1 = yc[c] + i * QK_K;
+            device const float * y2 = y1 + 128;
+            sumy[c] = 0.f;
+            for (short l = 0; l < 4; ++l) {
+                yl[c][l+0] = y1[l+ 0]; sumy[c][0] += yl[c][l+0];
+                yl[c][l+4] = y1[l+32]; sumy[c][1] += yl[c][l+4];
+                yh[c][l+0] = y2[l+ 0]; sumy[c][2] += yh[c][l+0];
+                yh[c][l+4] = y2[l+32]; sumy[c][3] += yh[c][l+4];
+            }
+        }
+
+        device const uint8_t * q1 = x[i].qs + q_offset;
+        device const uint8_t * qh = x[i].qh + l0;
+        device const uint4   * hp = (device const uint4 *) &x[i];
+
+        FOR_UNROLL (short row = 0; row < NR0; ++row) {
+            device const uint8_t * q2 = q1 + 64;
+
+            const uint4  hdr = *hp;
+            const half2  dm  = as_type<half2>(hdr.x);
+
+            const uint16_t s0 = (uint16_t) (hdr.y >> shs);
+            const uint16_t s2 = (uint16_t) (hdr.z >> shs);
+            const uint16_t s4 = (uint16_t) (hdr.w >> shs);
+
+            uint16_t sc16[4];
+            sc16[0] = s0 & kmask1;
+            sc16[1] = s2 & kmask1;
+            sc16[2] = ((s4 >> 0) & kmask2) | ((s0 & kmask3) >> 2);
+            sc16[3] = ((s4 >> 4) & kmask2) | ((s2 & kmask3) >> 2);
+            thread const uint8_t * sc8 = (thread const uint8_t *) sc16;
+
+            const uchar4 qv1 = as_type<uchar4>(*(device const uint *)(q1));
+            const uchar4 qv2 = as_type<uchar4>(*(device const uint *)(q2));
+            const uchar4 qvh = as_type<uchar4>((*(device const uint *)(qh) >> hsh) & 0x33333333u);
+
+            float qa[4], qb[4], qc[4], qd[4];
+            FOR_UNROLL (short l = 0; l < 4; ++l) {
+                const uint8_t h = qvh[l];
+                qa[l] = (qv1[l] & 0x0F) | ((h & 1) << 4);
+                qb[l] = (qv1[l] >>   4) | ((h & 2) << 3);
+                qc[l] = (qv2[l] & 0x0F) |  (h & 16);
+                qd[l] = (qv2[l] >>   4) | ((h & 32) >> 1);
+            }
+
+            FOR_UNROLL (short c = 0; c < NC; c++) {
+                float4 acc1 = {0.f, 0.f, 0.f, 0.f};
+                FOR_UNROLL (short l = 0; l < 4; ++l) {
+                    acc1[0] += yl[c][l+0] * qa[l];
+                    acc1[1] += yl[c][l+4] * qb[l];
+                    acc1[2] += yh[c][l+0] * qc[l];
+                    acc1[3] += yh[c][l+4] * qd[l];
+                }
+                sumf[row][c] += dm[0] * (sc8[0] * acc1[0] + sc8[1] * acc1[1] + sc8[4] * acc1[2] + sc8[5] * acc1[3]) -
+                                dm[1] * (sumy[c][0] * sc8[2] + sumy[c][1] * sc8[3] + sumy[c][2] * sc8[6] + sumy[c][3] * sc8[7]);
+            }
+
+            q1 += args.nb01;
+            qh += args.nb01;
+            hp  = (device const uint4 *) ((device const char *) hp + args.nb01);
+        }
+    }
+
+    FOR_UNROLL (short c = 0; c < NC; c++) {
+        if (r1 + c >= args.ne11) {
+            break;
+        }
+        device float * dst_f32 = (device float *) dst + (int64_t)im*args.ne0*args.ne1 + (int64_t)(r1 + c)*args.ne0;
+
+        for (int row = 0; row < NR0 && first_row + row < args.ne0; ++row) {
+            const float tot = simd_sum(sumf[row][c]);
+            if (tiisg == 0) {
+                dst_f32[first_row + row] = tot;
+            }
+        }
+    }
+}
+
+#define QK_MV_NC_W64(T, NR0, NC) \
+[[host_name("kernel_mul_mv_" #T "_f32_c" #NC "_r" #NR0 "_w64")]] \
+kernel void kernel_mul_mv_##T##_f32_c##NC##_r##NR0##_w64( \
+        constant ggml_metal_kargs_mul_mv & args, \
+        device const char * src0, \
+        device const char * src1, \
+        device       char * dst, \
+        uint3  tgpig[[threadgroup_position_in_grid]], \
+        ushort tiisg[[thread_index_in_simdgroup]], \
+        ushort sgitg[[simdgroup_index_in_threadgroup]]) { \
+    kernel_mul_mv_##T##_f32_nc_w64_impl<NR0, NC>(args, src0, src1, dst, tgpig, tiisg, sgitg); \
+}
+
+QK_MV_NC_W64(q5_K, 2, 2)
+QK_MV_NC_W64(q5_K, 4, 2)
+QK_MV_NC_W64(q5_K, 8, 2)
+
 [[host_name("kernel_mul_mv_q4_K_f32_w64")]]
 kernel void kernel_mul_mv_q4_K_f32_w64(
         constant ggml_metal_kargs_mul_mv & args,
         device const char * src0,
         device const char * src1,
         device       char * dst,
         uint3  tgpig[[threadgroup_position_in_grid]],
         ushort tiisg[[thread_index_in_simdgroup]],
