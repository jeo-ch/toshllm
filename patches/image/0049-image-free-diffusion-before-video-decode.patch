diff --git a/src/model_manager.cpp b/src/model_manager.cpp
index 930c6f4..bc6b1c4 100644
--- a/src/model_manager.cpp
+++ b/src/model_manager.cpp
@@ -1112,40 +1112,42 @@ void ModelManager::free_params_storage_block(ParamsStorageBlock& block) {
 
         state->loaded_to_params_backend = false;
         state->applied_lora_epoch       = UINT64_MAX;
     }
     block.states.clear();
 }
 
 void ModelManager::release_params_storage_blocks(bool force,
-                                                 const std::unordered_set<TensorState*>* target_states) {
+                                                 const std::unordered_set<TensorState*>* target_states,
+                                                 bool allow_resident) {
     struct ReleaseStats {
         size_t bytes   = 0;
         size_t tensors = 0;
         size_t blocks  = 0;
     };
     std::map<ggml_backend_buffer_type_t, ReleaseStats> released;
     for (auto it = params_storage_blocks_.begin(); it != params_storage_blocks_.end();) {
         ParamsStorageBlock* block = it->get();
         bool can_release          = force;
         if (!can_release) {
             can_release = std::all_of(block->states.begin(),
                                       block->states.end(),
-                                      [target_states](TensorState* state) {
+                                      [target_states, allow_resident](TensorState* state) {
                                           if (state == nullptr) {
                                               return true;
                                           }
                                           if (target_states != nullptr &&
                                               target_states->find(state) == target_states->end()) {
                                               return false;
                                           }
                                           return state->pin_count == 0 &&
                                                  !state->staged_to_compute_backend &&
-                                                 state->residency_mode == ResidencyMode::Disk;
+                                                 (allow_resident ||
+                                                  state->residency_mode == ResidencyMode::Disk);
                                       });
         }
 
         if (can_release) {
             if (block->buffer != nullptr) {
                 auto& stats = released[ggml_backend_buffer_get_type(block->buffer)];
                 stats.bytes += ggml_backend_buffer_get_size(block->buffer);
                 stats.tensors += block->states.size();
@@ -1476,16 +1478,30 @@ void ModelManager::finish_compute_backend_usage(const std::vector<TensorState*>&
             continue;
         }
         if (state->pin_count > 0) {
             state->pin_count--;
         }
     }
 }
 
+void ModelManager::unload_component(ModelComponent component) {
+    std::unordered_set<TensorState*> target_states;
+    for (const auto& state : tensor_states_) {
+        if (state != nullptr && state->component == component) {
+            target_states.insert(state.get());
+        }
+    }
+    if (target_states.empty()) {
+        return;
+    }
+    release_compute_staging_blocks(false, &target_states);
+    release_params_storage_blocks(false, &target_states, true);
+}
+
 void ModelManager::release_compute_backend_params(const std::vector<ggml_tensor*>& tensors) {
     if (tensors.empty()) {
         return;
     }
     std::vector<TensorState*> required_states;
     if (!resolve_required_tensor_states(tensors, required_states)) {
         return;
     }
diff --git a/src/model_manager.h b/src/model_manager.h
index f3a6e32..745c729 100644
--- a/src/model_manager.h
+++ b/src/model_manager.h
@@ -160,17 +160,18 @@ private:
                                  const std::vector<TensorState*>& states,
                                  bool log_details = false) const;
 
     ggml_backend_buffer_type_t params_buffer_type_for(const TensorState& state) const;
     ggml_backend_buffer_type_t split_buffer_type_for(const TensorState& state) const;
     void release_compute_staging_blocks(bool force                                            = false,
                                         const std::unordered_set<TensorState*>* target_states = nullptr);
     void release_params_storage_blocks(bool force                                            = false,
-                                       const std::unordered_set<TensorState*>* target_states = nullptr);
+                                       const std::unordered_set<TensorState*>* target_states = nullptr,
+                                       bool allow_resident                                   = false);
     void free_compute_staging_block(ComputeStagingBlock& block);
     void free_params_storage_block(ParamsStorageBlock& block);
     void erase_params_storage_block(ParamsStorageBlock* block);
     void reset_lora_applied_params();
     bool unregister_tensor_states(const std::unordered_set<TensorState*>& states, size_t* size);
     size_t other_runtime_resident_bytes(uintptr_t owner_id,
                                         ggml_backend_t compute_backend) const;
 
@@ -271,16 +272,20 @@ public:
     bool prepare_params(const std::vector<ggml_tensor*>& tensors) override;
     void set_workspace_reclaimer(uintptr_t owner_id, std::function<bool()> reclaim) override;
     void remove_runtime_owner(uintptr_t owner_id) override;
     bool fits_compute_backend_capacity(const DeviceMemoryRequest& request,
                                        const std::vector<ggml_tensor*>& required_params) const override;
     bool segmented_compute_enabled() const override { return !segmented_compute_disabled_; }
     bool prefetch_enabled() const override { return !prefetch_disabled_; }
     void release_compute_backend_params(const std::vector<ggml_tensor*>& tensors) override;
+
+    // hand back the video memory a component's weights hold, even when they are resident
+    // rather than streamed from disk. The caller has to know the component is done.
+    void unload_component(ModelComponent component);
     void evict_compute_backend_params(const std::vector<ggml_tensor*>& tensors) override;
     WeightResidencyInfo inspect_compute_backend_params(
         const std::vector<ggml_tensor*>& tensors) const override;
     void update_runtime_residency(uintptr_t owner_id,
                                   ggml_backend_t compute_backend,
                                   size_t resident_bytes) override;
     bool ensure_compute_backend_capacity(
         const DeviceMemoryRequest& request,
diff --git a/src/pipeline/diffusion_engine.cpp b/src/pipeline/diffusion_engine.cpp
index e020a2d..3144c04 100644
--- a/src/pipeline/diffusion_engine.cpp
+++ b/src/pipeline/diffusion_engine.cpp
@@ -198,16 +198,33 @@ void StableDiffusionGGML::end_runners() {
         control_net->runner_end();
     for (auto& extension : generation_extensions)
         extension->runner_end();
     for (auto& lora : runtime_lora_models)
         if (lora.model)
             lora.model->runner_end();
 }
 
+// The sampler keeps the diffusion weights in video memory until the run ends, and the decoder
+// then has to fit next to them: on a 12 GB card a 4 GB transformer leaves under 2 GB and the
+// VAE decode fails. Only call this once no sampling pass is left.
+void StableDiffusionGGML::release_diffusion_weights() {
+    if (model_manager == nullptr) {
+        return;
+    }
+    // by component: a finished runner cannot always list its own parameters
+    for (const std::shared_ptr<DiffusionModelRunner>& runner : {diffusion_model, high_noise_diffusion_model}) {
+        if (runner != nullptr) {
+            runner->runner_end();
+        }
+    }
+    model_manager->unload_component(ModelComponent::Diffusion);
+    model_manager->unload_component(ModelComponent::HighNoiseDiffusion);
+}
+
 bool StableDiffusionGGML::reset_runners(const RunnerGroups& groups) {
     end_runners();
     clear_lora_adapters();
     runtime_lora_models.clear();
     for (auto group : groups) {
         for (auto component : runner_components().at(group)) {
             if (!model_manager->unregister_param_tensors(component)) {
                 return false;
diff --git a/src/pipeline/diffusion_engine.h b/src/pipeline/diffusion_engine.h
index f2f8a8c..8663b1c 100644
--- a/src/pipeline/diffusion_engine.h
+++ b/src/pipeline/diffusion_engine.h
@@ -103,16 +103,19 @@ public:
 
     bool diffusion_conv_direct = false;
 
     bool is_using_v_parameterization     = false;
     bool is_using_edm_v_parameterization = false;
 
     std::shared_ptr<ModelManager> model_manager;
 
+    // Hand back the video memory the sampler's weights hold, once no sampling pass is left.
+    void release_diffusion_weights();
+
     enum class RunnerGroup { Core,
                              VAE,
                              ControlNet,
                              Extensions };
     using RunnerGroups = std::set<RunnerGroup>;
 
     struct ModelConfig {
         sd_ctx_params_t params{};
diff --git a/src/pipeline/image.cpp b/src/pipeline/image.cpp
index 4c90150..8eff2d2 100644
--- a/src/pipeline/image.cpp
+++ b/src/pipeline/image.cpp
@@ -1022,16 +1022,20 @@ namespace sd::pipeline {
                 return false;
             }
             int64_t hires_denoise_end = ggml_time_ms();
             LOG_INFO("hires fix completed, taking %.2fs", (hires_denoise_end - hires_denoise_start) * 1.0f / 1000);
 
             final_latents = std::move(hires_final_latents);
         }
 
+        // every sampling pass is done here, including the hires refine above, so give the
+        // decoder the room the sampler is holding
+        sd->release_diffusion_weights();
+
         int num_images = 0;
         auto result    = decode_image_outputs(sd, request, final_latents, &num_images);
         if (result == nullptr) {
             return false;
         }
 
         sd->lora_stat();
 
diff --git a/src/pipeline/video.cpp b/src/pipeline/video.cpp
index 503afa5..05b064c 100644
--- a/src/pipeline/video.cpp
+++ b/src/pipeline/video.cpp
@@ -1837,16 +1837,20 @@ namespace sd::pipeline {
             }
             LOG_INFO("sampling(latent upscale) completed, taking %.2fs",
                      (sampling_end - sampling_start) * 1.0f / 1000);
         }
 
         int64_t latent_end = ggml_time_ms();
         LOG_INFO("generating latent video completed, taking %.2fs", (latent_end - latent_start) * 1.0f / 1000);
 
+        // every sampling pass is done here, including the LTX upscale refine above, so give
+        // the decoder the room the sampler is holding
+        sd->release_diffusion_weights();
+
         sd_audio_t* generated_audio = nullptr;
         if (sd->version == VERSION_WAN2_2_S2V && sd_vid_gen_params->ref_audios_count > 0) {
             // Return the driving track for muxing with the generated video.
             const sd_audio_t& driving = sd_vid_gen_params->ref_audios[0];
             generated_audio           = (sd_audio_t*)malloc(sizeof(sd_audio_t));
             if (generated_audio != nullptr) {
                 generated_audio->sample_rate  = driving.sample_rate;
                 generated_audio->channels     = driving.channels;
